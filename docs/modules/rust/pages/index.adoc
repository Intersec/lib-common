= Rust in lib-common
:toc:

[.lead]
xref:rustdoc.adoc[*API documentation of the crates*]

Along with C, Rust is the other compiled language supported in lib-common. The
API documentation above is generated by
https://doc.rust-lang.org/rustdoc/[rustdoc], and is refreshed each time this
website is built.

== Rust code of lib-common

The Rust code of lib-common lives in the `rust/` directory of the repository.
It is not a rewrite of the C code but a wrapper around it: the C symbols are
exported to Rust with https://docs.rs/bindgen/[bindgen], and exposed in the
`bindings` module of the crates that need them. As a consequence, most crates
provide both a Rust API and the raw bindings of their C counterpart.

== Build system

`waf` remains the main build system: it schedules the build and calls `cargo`
for the packages producing artifacts used by the C code (`staticlib`, `cdylib`
and `bin`), while the packages producing only Rust libraries are entirely
handled by `cargo`. All the packages belong to a single Cargo workspace,
shared with the projects based on lib-common, and are listed in the `members`
field of its `Cargo.toml`.

Because the Rust code wraps the C one, the API documentation can only be
generated after the C libraries have been built:

[source,shell]
----
waf configure
waf
cargo doc --workspace --no-deps
----

== Further reading

* https://github.com/Intersec/lib-common/blob/master/README-rust.adoc[`README-rust.adoc`],
at the root of the repository, details the interaction between `waf` and
`cargo`.
* https://techtalk.intersec.com/2026/03/rust-in-lib-common-part-1-integrating-rust-in-a-waf-based-c-build-system/[Rust
in lib-common, part 1: integrating Rust in a waf-based C build system], on the
Intersec Tech Talk blog.
